[project]
name = "quick-sam"
version = "0.1.0"
description = "Interactive SAM 2.1 image segmentation with click points"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.5.1",
    "torchvision>=0.20.1",
    "sam2",
    "huggingface-hub",
    "matplotlib",
    "numpy",
    "pillow",
]
